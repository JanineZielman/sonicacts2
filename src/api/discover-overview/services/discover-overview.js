'use strict';

/**
 * discover-overview service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::discover-overview.discover-overview');
